# Deployments

`deploy.cjs` writes deployment receipts here. The files contain public contract addresses, tx hashes, sample Reality Ledger ids, and timestamps only. Do not store private keys, RPC credentials, or provider tokens here.
